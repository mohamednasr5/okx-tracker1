name: 🏗️ Build OKX Tracker APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build Wear OS APK
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: 🔧 Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.6'

      - name: 📦 Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: 📜 Accept Android licenses
        run: yes | sdkmanager --licenses || true

      - name: 🔑 Make gradlew executable
        run: chmod +x gradlew

      - name: 🏗️ Build Debug APK
        run: ./gradlew assembleDebug --no-daemon

      - name: 📤 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: OKX-Tracker-WearOS-APK
          path: app/build/outputs/apk/debug/app-debug.apk
          retention-days: 90

      - name: 📋 Summary
        run: |
          echo "## ✅ APK جاهز للتحميل!" >> $GITHUB_STEP_SUMMARY
          echo "**Actions → هذا الـ Run → Artifacts → OKX-Tracker-WearOS-APK**" >> $GITHUB_STEP_SUMMARY
          echo "🛠️ تصميم وبرمجة: م. محمد حماد" >> $GITHUB_STEP_SUMMARY
